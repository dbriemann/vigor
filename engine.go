package vigor

// TODO: this just collects ideas for the future framework.

// Stage holds a stack of scenes where the top is always active and drawn.
type Stage struct {
}

// Scene holds all data that is needed to represent a scene, be it a game view, a menu,
// a cut scene, etc.
type Scene struct {
}

type State struct {
}

type Camera struct {
}
